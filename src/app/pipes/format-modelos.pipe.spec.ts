import { FormatModelosPipe } from './format-modelos.pipe';

describe('FormatModelosPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatModelosPipe();
    expect(pipe).toBeTruthy();
  });
});
